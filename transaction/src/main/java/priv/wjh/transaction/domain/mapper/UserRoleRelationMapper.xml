<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="priv.wjh.transaction.domain.dao.UserRoleRelationMapper" >

    <sql id="tableName">
        user_role_relation
    </sql>



    <insert id="add">
        insert into <include refid="tableName" />(user_id, role_id)
        values
        <foreach collection="list" Item="Item" separator=",">
            (#{Item.userId}, #{Item.roleId})
        </foreach>
    </insert>

    <delete id="deleteByUserId">
        delete from <include refid="tableName" /> where user_id = #{id}
    </delete>

    <select id="find" resultType="priv.wjh.transaction.domain.po.UserRoleRelationPojo">
        select *
        from <include refid="tableName" />
        where user_id = #{id}
    </select>

</mapper>
